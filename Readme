In the following code, we have created the website using HTML, CSS and JavaScript. We have added the flask as python framework. We have done data scraping through web Harvy to compare products from different websites.
